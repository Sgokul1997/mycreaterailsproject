<%= form_for(mark) do |f| %>
  <% if mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark.errors.count, "error") %> prohibited this mark from being saved:</h2>

      <ul>
      <% mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tamil %>
    <%= f.number_field :tamil %>
  </div>

  <div class="field">
    <%= f.label :english %>
    <%= f.number_field :english %>
  </div>

  <div class="field">
    <%= f.label :maths %>
    <%= f.number_field :maths %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
